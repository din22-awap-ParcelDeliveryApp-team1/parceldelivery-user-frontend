/* import React from "react";
import { render, screen, fireEvent } from "@testing-library/react";
import "@testing-library/jest-dom/extend-expect";
import SendNewParcel from "../SendNewParcel";
// import child components
import Sidebar from "../../components/sidebar";
import ParcelSize from "../../components/ParcelSize";
import ParcelSizeImage from "../../components/ParcelSizeImage";
import SendParcelConfirm from "../../components/SendParcelConfirm";
import ReceiverSenderDetails from "../../components/ReceiverSenderDetails";
import { MemoryRouter } from "react-router"; */



// Mock any external dependencies or functions

/* test('renders Sidebar component', () => {
    render(<MemoryRouter><Sidebar /></MemoryRouter>); */
  
    // Example: Check if a component or text is present
    //expect(screen.getByText('My account')).toBeInTheDocument();
  
    // You can add more assertions based on your component's content and behavior
  /* }); */
  
  /*test('clicking "Next" button advances to the next step', () => {
    render(<SendNewParcel />);
  
    // Example: Clicking the "Next" button
    fireEvent.click(screen.getByText('Next'));
  
    // Example: Check if the component or text related to the next step is present
    expect(screen.getByText('Enter Receiver Details')).toBeInTheDocument();
  
    // Add more assertions based on your component's behavior
  });*/

  
  
  // Add more tests as needed to cover different scenarios 